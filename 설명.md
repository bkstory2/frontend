# BoardList.vue 동작 설명

## 1. http://localhost:9000/board?board_id=free 호출 시 BoardList.vue가 호출되는 과정

- 사용자가 위 URL로 접근하면, Vue Router가 /board 경로를 감지합니다.
- router/index.js에서 /board 경로는 BoardList 컴포넌트(BoardList.vue)와 연결되어 있습니다.
- 따라서 BoardList.vue 파일이 로드되어 화면에 렌더링됩니다.
- URL의 ?board_id=free 쿼리 파라미터는 BoardList.vue에서 this.$route.query.board_id로 접근할 수 있습니다.

## 2. BoardList.vue에서 서버 데이터 가져와서 리스트 뿌려지는 과정

- BoardList.vue의 created() 또는 mounted() 라이프사이클 훅에서 서버에 데이터를 요청합니다.
- 예를 들어 axios 또는 fetch를 사용해 API 호출을 합니다. (예: /api/board?board_id=free)
- 서버에서 게시글 목록 데이터를 응답하면, BoardList.vue의 data 속성(예: posts)에 저장합니다.
- v-for 디렉티브를 사용해 posts 배열을 반복 렌더링하여 게시글 리스트를 화면에 표시합니다.

### 전체 동작 순서
1. URL 진입 →
2. 라우터가 BoardList.vue 연결 →
3. 컴포넌트에서 서버 데이터 요청 →
4. 데이터 받아서 리스트 렌더링

---

## [2026-03-12] Board 관련 최신 폴더 구조 및 적용 내역

1. **폴더 구조 개선**
   - `src/pages/board/` 폴더 생성: BoardList.vue, DetailView.vue, WriteView.vue, UpdateView.vue 이동 및 관리
   - `src/components/board/` 폴더 생성(재사용 컴포넌트 분리 용도)
2. **유틸/공통 코드 분리**
   - `src/utils/boardUtils.js`에서 게시판명, 날짜 포맷 등 공통 함수 관리
   - boardApi, axiosInstance 분리
3. **각 Board 관련 파일 개선**
   - boardUtils 유틸 함수(getBoardTitle, formatDate) 사용
   - 불필요한 중복 코드/콘솔 제거
   - axiosInstance 기반 boardApi 사용
   - 최신 pages/board 구조에 맞게 파일 위치 이동

> 이 구조는 유지보수, 확장성, 협업에 최적화된 최신 Vue 프로젝트 구조입니다.

---
